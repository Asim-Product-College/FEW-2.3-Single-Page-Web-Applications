const h1 = <h1>Hello world</h1>;
// This is aa JS file! Despite what it looks like, the code doesn't actually contain any HTML at all.

// The part that looks like HTML, <h1>Hello world</h1>, is something called JSX.